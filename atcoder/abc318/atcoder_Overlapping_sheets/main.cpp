#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n, a[102], b[102], c[102], d[102];

    scanf("%d", &n);

    for(int i =0 ; i < n; ++i)
    {
        cin >> a[i] >> b[i] >> c[i] >> d[i];
    }

        	
}