#include <bits/stdc++.h>

using namespace std;

int main ()
{
    int N;
    vector<int> d;

    for(int i = 0; i <= N; ++i)
    {
        if(N%i == 0)
            d.push_back(i);
    }

    cin >> N;

    for(int i = 0; i <= N;  ++i)
    {
        
    }
}